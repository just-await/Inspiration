name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # Или master, посмотри как называется твоя ветка

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Папка, которую создал Vite
          branch: gh-pages # Ветка, куда полетит готовый сайт